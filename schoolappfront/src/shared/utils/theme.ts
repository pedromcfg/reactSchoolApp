import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#212529",
      contrastText: "#F5F5F5",
    },
  },
  typography: {
    fontFamily: "Arima",
    fontSize: 20,
  },
});
